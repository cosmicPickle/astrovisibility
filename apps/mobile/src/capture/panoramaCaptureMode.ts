// Rebuild after changing this value. Both implementations use the same draft,
// final panorama stitching and mask storage. No runtime or user-facing switch.
export const PANORAMA_CAPTURE_MODE: 'manual' | 'continuous' = 'continuous';
